// Preserved exactly as the original Pages Router stub — confirmed dead
// (nothing in the app links here; order-detail navigation goes to
// /order-detail/[orderid] instead) but migrated as-is per instruction not to
// make a judgment call to remove it during this migration.
export default function OrderDetails() {
  return <div>OrderDetails</div>;
}
