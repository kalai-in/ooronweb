import NotificationSettingPage from "@/components/pagecomponents/NotificationSettingPage";

// No MetaData in the original Pages Router version of this route either —
// preserved exactly (no metadata export here).
export default function NotificationSettingRoute() {
  return <NotificationSettingPage />;
}
